filter:
  paths: [src/*]
  excluded_paths: [vendor/*, tests/*]

build:
  environment:
    postgresql: false
    mysql: false
    redis: false
  nodes:
    php82:
      environment:
        php:
          version: 8.2
